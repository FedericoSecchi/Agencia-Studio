// Este archivo será reemplazado con el código JavaScript exacto del componente ScrollFloat de reactbits.dev
// Cursor pedirá al usuario pegarlo.

export default function ScrollFloat({ children }) {
  return <div>{children}</div>;
}
